export async function downloadInstagramVideo(url: string): Promise<{ videoPath: string }> {
  // ПОКА ЗАГЛУШКА
  // дальше сюда подключишь реальный парсер
  return {
    videoPath: "/tmp/mock-video.mp4"
  };
}
